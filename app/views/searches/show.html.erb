<h1>Search Results</h1>

<table>
  <tr>
    <th>Name</th>
    <th>First Appearance</th>
    <th></th>
  </tr>

  <% @search.characters.each do |c| %>
    <tr>
      <td><%= link_to c.name, c %></td>
      <td><%= c.issue.name %></td>
    </tr>
  <% end %>
</table>

